import 'package:flutter/material.dart';

class MostLiked extends StatelessWidget {
  const MostLiked({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Most Liked Content"),
    );
  }
}
